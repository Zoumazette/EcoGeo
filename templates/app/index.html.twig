{% extends 'base.html.twig' %}

{% block body %}
    
    <h1> Bienvenu ! </h1>

    <button class="btn btn-info" href="{{ path('security_login')}}">Se connecter</button>
    <button class="btn btn-info" href="{{ path('security_registration')}}">S'enregistrer</button>
    
{% endblock %}